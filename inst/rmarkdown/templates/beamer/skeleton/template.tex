%%% BIPS Beamer Presentation Template for RMarkdown
%%% Based on the BIPS beamer theme by Louis Dijkstra
\documentclass[$for(classoption)$$classoption$$sep$,$endfor$$if(aspectratio)$$if(classoption)$,$endif$aspectratio=$aspectratio$$endif$]{beamer}

%%% Font encoding
\usepackage{ifxetex,ifluatex}
\ifnum 0\ifxetex 1\fi\ifluatex 1\fi=0 % if pdftex
  \usepackage[$if(fontenc)$$fontenc$$else$T1$endif$]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp}
\else % if luatex or xetex
  \usepackage{unicode-math}
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi

%%% BIPS Theme
\usetheme[$for(themeoptions)$$themeoptions$$sep$,$endfor$]{BIPS}

%%% Math support
\usepackage{amsmath,amssymb}

%%% Pandoc tightlist support
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

%%% Syntax highlighting (provided by Pandoc)
$highlighting-macros$

%%% Graphics
$if(graphics)$
\usepackage{graphicx}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>\textheight0.8\textheight\else\Gin@nat@height\fi}
\makeatother
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}
$endif$

%%% Tables
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
$if(multirow)$
\usepackage{multirow}
$endif$
\usepackage{calc}

%%% Bibliography
$if(natbib)$
\usepackage[$natbiboptions$]{natbib}
\bibliographystyle{$if(biblio-style)$$biblio-style$$else$plainnat$endif$}
$endif$
$if(biblatex)$
\usepackage[$for(biblatexoptions)$$biblatexoptions$$sep$,$endfor$]{biblatex}
$for(bibliography)$
\addbibresource{$bibliography$}
$endfor$
$endif$

%%% CSL references (Pandoc default)
$if(csl-refs)$
\newlength{\cslhangindent}
\setlength{\cslhangindent}{1.5em}
\newlength{\csllabelwidth}
\setlength{\csllabelwidth}{3em}
\newenvironment{CSLReferences}[2]%
  {\begin{list}{}{%
  \setlength{\itemindent}{0pt}
  \setlength{\leftmargin}{0pt}
  \setlength{\parsep}{0pt}
  \setlength{\itemsep}{\parsep}}}
  {\end{list}}
\newcommand{\CSLBlock}[1]{#1\hfill\break}
\newcommand{\CSLLeftMargin}[1]{\parbox[t]{\csllabelwidth}{#1}}
\newcommand{\CSLRightInline}[1]{\parbox[t]{\linewidth - \csllabelwidth}{#1}\break}
\newcommand{\CSLIndent}[1]{\hspace{\cslhangindent}#1}
$endif$

%%% Metadata
\title{$title$}
$if(subtitle)$
\subtitle{$subtitle$}
$endif$
\author{$for(author)$$author$$sep$ \and $endfor$}
$if(contactauthor)$
\contactauthor{$contactauthor$}
$else$
\contactauthor{$author/first$}
$endif$
\date{$date$}
$if(occasion)$
\occasion{$occasion$}
$endif$
$if(email)$
\email{$email$}
$endif$
$if(institute)$
\institute{$for(institute)$$institute$$sep$ \and $endfor$}
$endif$

%%% Additional header includes
$for(header-includes)$
$header-includes$
$endfor$

\begin{document}

$if(title)$
\frame{\maketitle}
$endif$

$for(include-before)$
$include-before$
$endfor$

$body$

$if(thankstext)$
%%% Final slide with contact information
\thanksframe{$thankstext$}
$endif$

$if(natbib)$
$if(bibliography)$
\begin{frame}[allowframebreaks]{$if(biblio-title)$$biblio-title$$else$References$endif$}
  \bibliographytrue
  \bibliography{$for(bibliography)$$bibliography$$sep$,$endfor$}
\end{frame}
$endif$
$endif$
$if(biblatex)$
\begin{frame}[allowframebreaks]{$if(biblio-title)$$biblio-title$$else$References$endif$}
  \bibliographytrue
  \printbibliography[heading=none]
\end{frame}
$endif$

$for(include-after)$
$include-after$
$endfor$

\end{document}
